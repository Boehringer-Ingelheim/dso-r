# And the \[\[ operator: to add secure list calling so that it cannot return NULL when call does not exist

And the \[\[ operator: to add secure list calling so that it cannot
return NULL when call does not exist

## Usage

``` r
# S3 method for class 'dsoParams'
x[[i, ...]]
```

## Arguments

- x:

  dsoParams object

- i:

  index for \`\[\[\` operator

- ...:

  additional arguments passed to the \`\[\[\` operator
