# Reload dsoParams

Reloads the current dsoParams configuration into the object.

## Usage

``` r
reload(params, env = rlang::caller_env())
```

## Arguments

- params:

  dsoParams object

- env:

  environment in which object is located, caller_env() by default

## Value

The updated dsoParams object.
