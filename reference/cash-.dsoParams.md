# overriding the the \$ operator to add secure list calling so that it cannot return NULL when call does not exist

overriding the the \$ operator to add secure list calling so that it
cannot return NULL when call does not exist

## Usage

``` r
# S3 method for class 'dsoParams'
x$name
```

## Arguments

- x:

  dsoParams object

- name:

  field name
